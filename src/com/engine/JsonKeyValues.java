package com.engine;

public enum JsonKeyValues {
	attack, card_type, cards, defense, icon, index, level, linkRating, lore, monsterAttribute, monsterType, name, pendulumLevel,
	pendulumLore, rank, summonRequirment, type;
}
