package com.engine;

public enum JsonKeyValues {
	attack, card_type, cards, defense, icon, index, level, linkArrow, linkRating, lore, monsterAttribute, monsterType, name, pendulumLevel,
	pendulumLore, rank, summonRequirement, type;
}
